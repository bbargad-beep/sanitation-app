streamlit>=1.32.0
starlette>=0.45.0,<0.46.0
streamlit-aggrid
pandas
numpy
plotly
openpyxl
xlsxwriter
requests
geopy
pyproj
scikit-learn
playwright

# Local municipal address database (data/address_points/ktovot_point_hrz.shp).
# Without these, geocode_pipeline._load_local_db() silently disables the
# offline address pass and every lookup falls through to remote APIs.
geopandas
shapely
pyogrio
